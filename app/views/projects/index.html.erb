<% title "Projects" %>

<table>
  <tr>
    <th>Title</th>
    <th>Client</th>
    <th>Due Date</th>
    <th>Collaborators</th>
    <th>Tickets</th>
    <th>&nbsp;</th>
    <th>&nbsp;</th>
    <th>&nbsp;</th>
  </tr>
  <% for project in @projects %>
    <tr>
      <td><%=h project.title %></td>
      <td><%=h project.client_id %></td>
      <td><%= project.due_date %></td>
      <td><%=h project.collaborators %></td>
      <td><small>Tickets: <%= link_to project.tickets.size, project_tickets_path(project) %></small></td>
      <td><%= link_to "Show", project %></td>
      <td><%= link_to "Edit", edit_project_path(project) %></td>
      <td><%= link_to "Destroy", project, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= button_to "New Project", @client ? new_client_project_path(@client) : new_project_path, :class => "button" %></p>
